name: 15-minute-cron
on:
  schedule:
    - cron: '*/15 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Update progress data
        run: |
          curl --request GET \
          --url 'https://bransan.vercel.app/api/update'
